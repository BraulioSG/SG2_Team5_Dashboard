---
const { width, height } = Astro.props;
---

<svg id="chart-area" width={width} height={height}></svg> 

<script>
    const svg = d3.select("#chart-area");
    const margin = { left: 100, right: 10, top: 10, bottom: 100};
    (async ()=>{
        const data = await d3.csv("/data/results.csv")
        console.log(data);
    })()
</script>
